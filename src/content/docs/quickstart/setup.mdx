---
title: Quick Start — ZED + Claude Code Tab
description: In 5 Minuten zum laufenden Collective Context Setup
---

import { Aside, LinkCard, Steps } from '@astrojs/starlight/components';

## Voraussetzungen

- Linux / macOS / Windows (ZED läuft auf allen)
- Claude Max Abo (claude.ai) — für Flat Rate
- Node.js 18+ (für den ACP-Adapter)

<Steps>

1. **ZED installieren**

   Download unter: [zed.dev/download](https://zed.dev/download)

   ZED ist kostenlos und Open Source (GPL). Kein Account erforderlich für die Basis-Nutzung.

2. **Claude Code Tab aktivieren**

   - Agent Panel öffnen: `Ctrl+?`
   - `+` Button klicken → **Claude Agent** (aus ACP Registry auswählen)
   - ZED installiert `@zed-industries/claude-agent-acp` automatisch
   - `/login` eingeben → "Log in with Claude.ai" → OAuth im Browser abschließen
   - Fertig — Claude Code Tab ist aktiv.

3. **CLAUDE.md anlegen**

   Im Projekt-Root eine Datei `CLAUDE.md` anlegen:

   ```markdown
   # CLAUDE.md

   ## Projekt
   [Kurzbeschreibung des Projekts, max. 3 Sätze]

   ## Entscheidungen (nicht mehr hinterfragen)
   - [Architektur-Entscheidung 1]
   - [Architektur-Entscheidung 2]

   ## Was Claude NICHT tun soll
   - [Anti-Pattern 1 aus schlechter Erfahrung]
   - [Anti-Pattern 2]

   ## Commit-Format
   feat: / fix: / docs: (Conventional Commits)
   ```

   <Aside type="tip">
   CLAUDE.md wird **automatisch** in jede neue Session geladen — kein manueller Upload nötig.
   </Aside>

4. **Ersten Task starten**

   Im Agent Panel (Claude Code Tab aktiv):

   ```
   Lies CLAUDE.md und erkläre mir die Projektstruktur
   ```

   Claude liest automatisch CLAUDE.md, AGENTS.md (falls vorhanden) und scannt den Codebase.

5. **Postbox für Multi-Agent einrichten** *(optional)*

   Für den [Dual-Agent Workflow](/patterns/dual-agent) mit Gemini CLI:

   ```bash
   mkdir postbox
   touch postbox/todo.md postbox/done.md
   ```

   ```markdown
   # postbox/todo.md

   ## Offene Tasks

   | ID | Task | Priorität | Quelle |
   |----|------|-----------|--------|
   | #001 | Beispiel-Task | hoch | Gemini |
   ```

</Steps>

## Nächste Schritte

<LinkCard
  title="Was ist ACP?"
  href="/quickstart/what-is-acp"
  description="Konzeptueller Hintergrund — wie das Agent Client Protocol funktioniert."
/>
<LinkCard
  title="Claude Code Tab — alle Features"
  href="/zed/claude-code-tab"
  description="Deep-Dive in den ZED Claude Code Tab: Diffs, Hooks, MCP, Sub-Agents."
/>
<LinkCard
  title="CLAUDE.md Best Practices"
  href="/cc/claude-md"
  description="Das Projekt-Gedächtnis richtig schreiben — Struktur, Tiefe, Anti-Patterns."
/>
<LinkCard
  title="Dual-Agent Pattern einrichten"
  href="/patterns/dual-agent"
  description="Gemini CLI als Scanner + Claude Code als Fixer — der produktivste Workflow."
/>
